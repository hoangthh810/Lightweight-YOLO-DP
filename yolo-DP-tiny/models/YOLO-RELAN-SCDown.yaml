nc: 1
depth_multiple: 0.33
width_multiple: 0.50
anchors:
  - [10, 13, 16, 30, 33, 23]
  - [30, 61, 62, 45, 59, 119]

backbone: [
    [-1, 1, Conv,         [64, 6, 2, 2]],       # 0-P1/2
    [-1, 1, Conv,         [128, 3, 2]],           # 1-P2/4
    [-1, 1, GhostConv,    [128]],                 # 2
    [-1, 1, SCDown, [256]],      # 3-P3/8 (Downsample)
    [-1, 1, GhostConv,    [256]],                # 4  ← P3 skip
    [-1, 1, SCDown, [512]],      # 5-P4/16 (Downsample)
    [-1, 1, GhostConv_LR, [512]],                # 6  ← P4 skip
    [-1, 1, SPPF_LR,      [512, 5, 32]],          # 7  (giữ nguyên)
  ]

head: [
    [-1, 1, GhostConv_LR, [256, 1, 1, 16]],     # 8  (giữ nguyên)
    [-1, 1, nn.Upsample,  [None, 2, 'nearest']], # 9
    [[-1, 4], 1, Concat,  [1]],                   # 10
    [-1, 1, RELAN,         [256, False]],            # 11 ← [3] R-ELAN (Feb 2025)

    [-1, 1, SCDown, [256]],      # 12
    [[-1, 8], 1, Concat,  [1]],                   # 13
    [-1, 1, RELAN,         [512, False]],            # 14 ← [3] R-ELAN (Feb 2025)

    [[11, 14], 1, Detect, [nc, anchors]],
  ]
